# Image-to-Speech Caption Generator
Flask web application that converts images to text captions using BLIP and generates audio narration using text-to-speech.

```bash
pip install -r requirements.txt

python3 train_model.py --num_images 2000 --epochs 10

python3 app.py
```
